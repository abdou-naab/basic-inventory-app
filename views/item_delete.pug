extends layout

block content

  h1 #{title}: #{item.name + ', ' + item.nis + ' in stock.'}

  p Do you really want to delete this Item?

    form(method='POST')
      div
        label(for='pass') Password:
        input(type='password', placeholder='password to delete item' name='pass' required )
      div
        input(type='hidden', name='item_id', value=item._id )

      div 
        if errors 
          ul.err
            for e in errors
              li!= e.msg
      button(type='submit') Delete

